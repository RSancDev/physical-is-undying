# Do not put private provider keys in VITE_* variables for a public GitHub Pages build.
# VITE_* values are compiled into frontend assets and can be inspected by users.
VITE_TMDB_PROXY_URL=
VITE_PROVIDER_PROXY_URL=
VITE_DISQ_ENDPOINT=https://product.disqapis.com/graphql
